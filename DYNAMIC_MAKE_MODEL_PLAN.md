
# Dynamic Make & Model Implementation Plan

## 1. Database Schema (New Table)
We will create a lookup table for vehicles.

**SQL Migration:**
```sql
CREATE TABLE public.vehicle_catalog (
    id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    make text NOT NULL,
    model text NOT NULL,
    created_at timestamp with time zone DEFAULT timezone('utc'::text, now()) NOT NULL,
    UNIQUE(make, model)
);

-- Enable RLS (Public Read-Only)
ALTER TABLE public.vehicle_catalog ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow public read access" ON public.vehicle_catalog
    FOR SELECT USING (true);

-- Indexes for performance
CREATE INDEX idx_vehicle_catalog_make ON public.vehicle_catalog(make);
```

## 2. Data Seeding Strategy
We will parse the existing CSV file `public/cars_sold_2015_2025_expanded_no_series_class_no_duplicates_-_cars_sold_2015_2025_expanded_no_series_class_no_duplicates.csv` and insert it into the database.

**Approach:**
- Create a utility component/script that reads the CSV.
- Batch insert the data into Supabase `vehicle_catalog`.
- This ensures the DB matches your specific CSV exactly.

## 3. Component Updates (`Step1_VehicleData.tsx`)

**A. Remove Hardcoded Data:**
- Delete `const brands = [...]`

**B. Implement `useVehicleData` Hook:**
- `useMakes()`: Fetches unique brands `SELECT DISTINCT make`.
- `useModels(selectedMake)`: Fetches models where `make === selectedMake`.

**C. UI Improvements:**
- **Make Field**: Convert to Searchable Combobox (Shadcn `Command` + `Popover`).
- **Model Field**: Convert `Input` to Searchable Combobox.
- **Logic**:
    - Disable Model field if no Make is selected.
    - Clear Model value when Make changes.
    - Show "Loading..." state while fetching models.

## 4. Implementation Steps

1.  **Switch to Creative Mode**.
2.  Execute SQL to create `vehicle_catalog`.
3.  Run the seed script to populate data from your CSV.
4.  Modify `Step1_VehicleData.tsx` to use the new dynamic data.
