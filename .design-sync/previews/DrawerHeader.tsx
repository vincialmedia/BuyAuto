import { DrawerPanel } from './_drawer-demo';

// DrawerHeader is the padded, centred-on-mobile text block at the top of a
// drawer. See _drawer-demo.tsx for why the panel around it is scaffolding.
export function InDrawerPanel() {
  return <DrawerPanel />;
}
