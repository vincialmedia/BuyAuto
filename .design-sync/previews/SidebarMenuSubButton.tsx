import { NestedNav } from './_nested-nav';

// SidebarMenuSubButton is the clickable row inside SidebarMenuSub. The shared
// composition already shows both its active and inactive states.
export function InNestedNav() {
  return <NestedNav />;
}
