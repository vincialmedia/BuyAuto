import { DrawerPanel } from './_drawer-demo';

// DrawerFooter is the stacked action block at the bottom of a drawer. Shares
// DrawerHeader's composition — see _drawer-demo.tsx.
export function InDrawerPanel() {
  return <DrawerPanel />;
}
