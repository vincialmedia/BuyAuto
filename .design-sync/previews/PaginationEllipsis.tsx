import { Pager } from './_pager-demo';

// PaginationEllipsis marks the collapsed gap between page ranges — three dots
// plus a screen-reader label, so it only reads as anything inside a pager.
export function InPager() {
  return <Pager />;
}
