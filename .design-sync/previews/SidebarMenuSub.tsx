import { NestedNav } from './_nested-nav';

// SidebarMenuSub is the indented nested list under a menu item — it draws the
// left rule its children hang off, so it needs both a parent item and children.
export function InNestedNav() {
  return <NestedNav />;
}
